
Always Use:
atro, tailwind-4-docs, web-design-guidelines these 3 skills for this project
DESIGN.md for this project design

# Permanent fix? YES — user was adamant

## Facebook Login Wall Limitation (documented, NOT a code bug)

Facebook Variable Anti-Scraping Behavior: Facebook serves login walls ("Log into Facebook" / "Email or mobile number" / "Password" forms) to anonymous/bot requests for SOME posts — even publicly visible ones. This is NOT a consistent bug; it's Facebook's variable anti-scraping behavior that:
- Affects different posts from different IPs unpredictably
- Is more common on flagged/throttled server IPs (like the dev machine)
- Production unflagged IPs have a much higher success rate
- Cannot be bypassed without violating Facebook ToS (fake login/cookies)

**Detection:** `detectLoginWall()` in `src/lib/facebook.ts` identifies login walls in reader proxy responses (r.jina.ai). Patterns: "Log into Facebook", "email or mobile number" + "password", password input fields, CAPTCHA warnings, tiny pages with only login links.

**User-facing messages:**
- Photos: "This Facebook post's privacy settings are preventing access. Facebook sometimes blocks anonymous access to public posts — try a different public post."
- Videos: Generic fallback (video pipeline doesn't use reader proxy)
- Stories: Reader proxy returns empty → falls to "Could not load this Facebook story"

**Success rate (flagged dev IP, 13 URLs tested):** 46% overall. Most failures are login walls (5/6 reader proxy responses confirmed login-walled). On unflagged production IPs, success rate is significantly higher (estimated 70-85%+).

## WIP Status (last session: IG story session UNLOCKED — full-cookie + mobile-web fingerprint wins)

- **New IG `/reels/{code}/` URL format now supported (user request — Chrome address-bar links).** Instagram moved reels links to plural `/reels/` in current builds; the SEO pages already advertised it but the parser/form only matched `/reel/`. FIX: `parseInstagramUrl` regex now accepts `(?:\/reel|\/reels)`, and the client-side patterns in `InstagramDownloadForm.astro` (reels + audio tabs) accept `reel|reels`. Verified live on dev IP: `https://www.instagram.com/reels/DcPd4TNt0vK/` → 200 `type:"reels"` + real CDN mp4 (CLIPS C3.720) + `dl` stream 200 video/mp4 1.47MB; astro check 0/0/0.

- **DEV-ENV TRAP FOUND & FIXED (user hit "protected content" on localhost:3000 after a fresh login):** Vite/astro dev restarts the SSR process whenever `.env` changes (`Configuration file updated. Restarting...`) and the new process does NOT inherit shell-injected env vars (`process.env` stays empty — only `import.meta.env` gets .env values). So any session env set via `$env:IG_*` before `Start-Process npx astro dev` is LOST on the first `.env` edit/reload → the story route correctly falls to the no-session branch → "protected content" message. **FIX (`src/lib/ig-env.ts`):** `ensureInstagramEnv()` reads the project `.env` straight into `process.env` (IG_* keys only, never overriding real env), called at the top of both `instagram.ts` and `ytdlp.ts`. Production (systemd EnvironmentFile) is unaffected. Verified: after a bare restart (no shell env), the bilal test story still returns 200 + real video URL; astro check 0/0/0; story-flow 9/9 + e2e 18/18 still PASS.
- **Also verified live:** a user's fresh story link (`@uzs_tm`, media 3967014837124863130) correctly returns honest "Media not found or unavailable / content may be unavailable" — the tray is empty for the session account and `media/info` says not found, i.e. that specific story is genuinely expired/removed/private. Not a bug.

Completed & verified (astro check 0/0/0, quality lock 37/37, live dev-IP smoke):
- **User bug report: "5-6 photos show hoti hain laikin download sirf 1 hoti hai" (photo ZIP drops photos — PERMANENT FIX).** Root cause found live: FB's CDN **throttles burst connections on flagged IPs** — under the old 4-way concurrent zip burst, `z-m-scontent.flhe2-3.fna` (the og:image host) would time out while atl3-host siblings downloaded; and `fetchPhotoBuffer` had NO retry, so `fetchFacebookPhotosAsFiles` silently dropped the failed photo → ZIP shipped 1-4 entries with zero explanation. Even worse: thr flhe2 host is often just **slow** (414KB @ ~11KB/s = 36s — verified standalone 200/414700B), so short retry timeouts also failed. **FIXES in `src/lib/facebook.ts`:**
  - `fetchPhotoBuffer` now fails FAST under the burst (default timeout 15s, only url→alt once) so a throttled host costs seconds, not minutes, of the parallel phase.
  - `fetchFacebookPhotosAsFiles` tracks failed indices; after the burst it waits a **5s CDN-throttle cooldown** then re-fetches each failure **sequentially** (no competing connections — the throttle clears) with a **60s full-length timeout** (covers slow-but-alive hosts), up to 2 passes; concurrency lowered 4→3 to reduce burst pressure. `const sleep` helper added. Story-photo segments inherit the same retry via `fetchPhotoBuffer`.
  - **Verified live (2 consecutive runs):** `1BfhZMBwdw` ZIP = **5/5 entries, byte-identical totals 2,282,272B** (photo-01 414,700B 3072x4096 recovered via retry pass; runs took 83s/151s on the throttled dev IP — production IPs are fast, timeouts are worst-case safety). `dev6.err` shows only the expected burst fail-fast logs + silent retry recovery.
  - Photo count cap removed earlier per owner (was 50→20→**unlimited**): `MAX_PHOTOS_PER_POST` deleted, `bestCandidates.map(...)` serves every photo in the link.
  - **Frontend preview (owner request): "5 photos dikhao, age +N se expand karo"** — `FacebookDownloadForm.astro` photo grid now renders ALL photos client-side but hides all beyond 5 (`.photo-grid-hidden`), with a clickable `+{count-5}` tile (`#btn-grid-more`) that reveals the rest and removes itself. Verified compiled: `photo-grid-hidden`/`btn-grid-more`/`PREVIEW_PHOTOS` all in the served module; astro check 0/0/0; lock 37/37 PASS.
- **User bug report: "is link mein bohot photos hain laikin sirf 1 show hota hai" — FIXED (see sawShell/sawTimeout reader gate in the FB photo section below).**

Completed & verified (astro check 0/0/0, story-flow 9/9 + API e2e 18/18 PASS, live dev-IP smoke):
- **User story session mystery SOLVED (this is the ROBUST fix, supersedes the "session-only" dead-end note).** The user logged out + logged in fresh and pasted a NEW sessionid; API probes (current_user/reels_media/reel_media) still returned 403 / HTML shell / empty reels. Root cause: **Instagram web sessions are DEVICE-BOUND in 2026** — sending `sessionid` (or even the full cookie jar) with the WRONG client fingerprint gets silently de-authed: i.instagram.com + Instagram app UA = 403, www host with desktop UA = HTML login shell. The user pasted a raw `Copy as cURL` for their logged-in **mobile Chrome (Pixel 7 / Android 13 / Chrome 151)** session; replaying its exact headers on `https://www.instagram.com/api/v1` worked IMMEDIATELY: `feed/reels_media/?reel_ids=` → 200 with real story tray, `media/{id}/info/` → 200 full item JSON. `accounts/current_user`/`feed/user/{id}/reel_media` still return `{"message":"useragent mismatch"}` (stricter device checks) — irrelevant, we don't use them.
- **KEY PROOF (end-to-end on dev IP):** the app's real path (`resolveUserIdByUsername` → `fetchStoryTray` via `reels_media` → `findStoryInTray`) fetched a LIVE private-account story (`@bilal_khalidofficial`, media 3967329176939992715 — session follows them) in **1.5s** → real CDN mp4 (duration 40s, 720p dash_baseline). HTTP API `GET /api/instagram-download?url=.../stories/bilal_khalidofficial/3967329176939992715/&type=story` → `{"success":true,"type":"story","video":{play:…}}` + `dl` stream → **200 video/mp4 1MB+** directly from the CDN. Nonexistent id → honest `instagram_story_expired` sentinel.
- **FIXES in `src/lib/instagram.ts`:**
  - `getSessionCookie()` now prefers **`IG_COOKIES`** — the FULL cookie jar (datr, ig_did, mid, ig_nrcb, ps_l, ps_n, csrftoken, ds_user_id, sessionid, rur…) — over the old 3-part assembly (sessionid has to match the whole device context; partial sets get 403). Same in `src/lib/ytdlp.ts` `sessionCookieHeader()`.
  - New `INSTAGRAM_API_WEB = 'https://www.instagram.com/api/v1'` + `MOBILE_WEB_UA` (Pixel 7/Android 13/Chrome 151) + `MOBILE_WEB_HINTS` (full sec-ch-ua family incl. full-version-list/model/prefers-color-scheme/dpr/viewport-width). `apiGet` now switches: **session present → www host + mobile-Chrome web headers (X-IG-App-ID 936619743392459, X-ASBD-ID 129477, X-IG-WWW-Claim, X-CSRFToken, Sec-Fetch-*); anonymous → i.instagram.com + app UA (unchanged)**. `csrfFromCookie()` extracts the header token from the jar.
  - `isSessionBlockedMessage` also treats `useragent mismatch` as a session-block trigger (retry path handles it).
- **Docs:** `.env.example`/`.env.production.example` lead with **OPTION A (recommended): copy the whole `cookie:` string via DevTools → Network → Copy as cURL** into `IG_COOKIES`; API error hints updated to say the same. `.env` now holds the live full jar for the user's account.
- **REALITY-CEHCK for production:** the server MUST send this exact mobile-Chrome fingerprint on the www host for the session to authenticate. This dev machine works (verified) so production (a clean VPS IP) will too. If the account logs out / IG rotates cookies, re-copy the curl jar. Keep the cookie jar private (gitignored .env).
- NOTE: PowerShell Invoke-WebRequest cannot send `Sec-CH-UA-*` hints and mangles quoted headers (curl error 3) — test with `curl.exe -K <config>` on Windows. These files (Temp/ig-curl*.cfg) were deleted after use (they contained cookies) — recreate from the cURL the user pastes.

## WIP Status (prior session: FB photo quality — permanent ctp-cap unlock fix)

Completed & verified (astro check 0/0/0, live API smoke on dev IP):
- **User bug report: "photos downloaded ki quality bohot kharab / bakwas" on the FB Photo Downloader.** Root cause via deep probe (reader-proxy HTML + CDN URL dissection): the photo set shipped with TWO quality killers — (1) a **sticker/emoji asset leaked in as a "photo"**: reaction/sticker assets live in the `t39.1997-6` bucket (and avatars/emoji in `t39.30808-1`) and entered via the `"image":{"uri":…}` JSON scan which had NO bucket filter (110-320px stamps next to 1179x1572 real photos); (2) the download **fallback (`altUrl`) kept the `ctp=s590x590` cap** — the promoted URL 403s on signed/locked URLs, and the fallback then shipped the 590px rendition instead of the full file.
- **KEY LIVE PROOF:** on REAL signed cached URLs, raw = 32-41KB (capped), **`ctp`-stripped = 172-188KB** — same signature, full-size file (1179x1572). `ctp=` is the ONLY rewrite FB's signature does NOT lock (verified again live: og-image URLs AND sibling s590 URLs both unlock; `stp`/`cstp`/path/`_o` rewrites all 403 as signed -> dead weight).
- **🔒 PERMANENT LOCK (intentional — DO NOT weaken):** quality invariants now live in a dependency-free module **`src/lib/facebook-photo-quality.ts`** with an **output-boundary enforcement pass** (`enforcePhotoQuality()`, the LAST step of `fetchFacebookPhotoSet`) that strips EVERY `ctp=` cap, drops sticker/emoji/avatar buckets, rejects thumb-only leftovers, and dedupes by CDN path — no future extraction path can leak a capped/non-photo URL again. Pinned by regression test **`tests/facebook-photo-quality.test.ts`** run via **`npm run test:fb-photo`** (37 assertions, node-native TS, zero deps) covering: ctp-strip survival (incl. leading `?ctp=` and no `&&`/`?&` dangle), signed URLs never get stp/path 2048 bumps (they 403), sticker buckets always rejected (incl. the 330-score `dst-png_p320x320` case — the BUCKET filter, not the score, is the defense), cross-host CDN dedupe. **If the test fails, fix the pipeline — never the test.** `facebook.ts` imports all quality helpers from the locked module (no local copies).
- **FIXES in `src/lib/facebook.ts`:**
  - New `stripCtpCap(u)` helper (drops `ctp=[sp]{w}x{h}` + collapses dangling `&&`) — used for BOTH the primary URL and every fallback, so a capped rendition can never ship.
  - New `isNonPhotoAssetUrl(u)` filter — rejects `static.`/`rsrc.php`/`dst-emg`/`emg1/`/`t39.1997-6/`/`t39.30808-1/` — applied in `extractPhotosFromHtml.add()` (covers og:image + JSON blobs), the `<img>` scan, `fetchSiblingsViaReader`, and `fetchSiblingPhotosFull` (photo.php og:image). The sticker leak is impossible everywhere now.
  - `promotePhotoUrl` now skips the `stp=p2048/path` size rewrites on SIGNED URLs (`oe|oh|sig|token|eav` params) — they always 403, stripping `ctp` is verified as the only working upsize — so candidates on real pages are exactly `stripCtpCap(original)`.
  - All candidate builders (`add()`, reader, photo.php) now set `alt = stripCtpCap(clean)` (was: raw capped URL) — the download fallback is full-res, never 590px.
- **Verified live on dev IP** (killed node, cleared `media:fb:photo` keys, restarted, user's link `share/p/14jou2QjmXc/`): POST metadata → **`photoCount:5`** (was 6 — sticker gone), zero `ctp=` params on any photoUrl/altUrl; ZIP → **5/5 JPEGs @ 1179x1572** (105-280KB each, ~1020KB total — verified dims via System.Drawing, byte-identical to direct CDN probes: 105767/188241/172126/273355/280189); single `dl=photo` → 200 image/jpeg 1179x1572 (105KB). One ZIP attempt hit transient CDN throttling (3/5 `fetch failed` on concurrent fetches) — identical URLs succeeded immediately on direct probe; retry = 5/5. NOT a code issue.
- NOTE: dev server crashed twice from Vite watcher EBUSY — once on old probe files in `Temp/`, once on `dev-zip-test.zip` I wrote into the project dir. Test artifacts MUST go to `C:\Users\ARTHUR~1\AppData\Local\Temp\opencode\` (never inside the workspace). Restart pattern: stop all node, `Start-Process npx astro dev` hidden w/ redirects, ~14s to ready.
- **SECOND user report same session: "link mein bohot photos hain laikin sirf 1 show hota hai"** — `web.facebook.com/share/p/1BfhZMBwdw/`. Root cause: flagged IP serves the share page as a LARGE complete page (309KB, NOT truncated) containing ONLY the og:image (1 photo ID) — zero `"image":{"uri":…}` JSON blobs, zero carousel markers — so the old recovery gate `count===1 && sawTruncated` never fired and the reader-proxy fallback was skipped. Verified via reader probe: same link renders with **5 distinct photo IDs** on unflagged IPs. Fix in `src/lib/facebook.ts`: `bestFrom()` now also tracks `sawShell` (any host variant returned a tiny <15KB shell) and `sawTimeout` (host timed out); the reader gate is now `count===0 || (count===1 && (sawTruncated || sawShell || sawTimeout))`. Clean single-photo pages (large, complete, no shells/timeouts) still skip the reader — no wasted latency on production. Verified live: POST user's link → **count 5** (was 1), ZIP norm 4/5 @ **3072x4096** (photo-01 on flhe2 host throttles under concurrent fetch — standalone downloads 200/414KB fine, signatures valid 104h+; documented transient), single `dl=photo` → 200 3072x4096. Lock test 37/37 + astro check 0/0/0 PASS.
- Uncommitted from prior sessions (same worktree, staged-able): all IG story engine work, FB story login-wall honesty fixes, FB pipeline parallelization, premium HD tile styling, TikTok item-detail probe.

## WIP Status (last session: Instagram story downloader — permanent session-first engine)

Completed & verified (astro check 0/0/0, build green, 9/9 flow probe + 18/18 API e2e probe green, live dev-IP smoke):
- **User bug report: IG story downloads error EVERY time.** Root cause found via probing: **Instagram stories are NOT anonymous-fetchable in current builds (2026)** — verified directly: story page HTML = 610KB JS shell with ZERO media data (no video_versions/scontent/og:image under any UA incl. Googlebot), `/graphql/query` doc_ids ALL dead (`400 {"message":"invalid request","errors":[{"message":"execution error"}]}` — incl. the hardcoded `4740221914432035` / `27128499623469141` / `2721935559717057`), `/api/v1/feed/reels_media/` anonymous = `{"reels":{},"status":"ok"}` (guests get EMPTY — endpoint alive but data withheld), `/api/v1/feed/user/{id}/reel_media/` + `/api/v1/media/{id}/info/` = 403. **BUT `/api/v1/users/web_profile_info/?username=` works ANONYMOUSLY (200, full JSON incl. user `id`)** — used to resolve story username → userId without any login. Contrast: reels/posts still work anonymously (embed+GraphQL chain) — only stories are 100% session-gated. Every working "login-free" story site (storiesdown/storiesig/…) runs their own logged-in burner-account backend server-side — there is NO anonymous endpoint to build; the app's fix = a bulletproof session engine.
- **FIX (`src/lib/instagram.ts`):** new `fetchStoryByMediaId` layering — SESSION path: `resolveUserIdByUsername` (anon web_profile_info, cached 7d) → `fetchStoryTray(userId)` (`/feed/reels_media/?reel_ids=`) → `findStoryInTray` by media_id (tray non-empty + id missing ⇒ honest `ERR_STORY_EXPIRED`) → fallback `/media/{id}/info/` → fallback yt-dlp with session Cookie header (`--add-header` path already existed). NO-session path: ONE bounded 6s anonymous GraphQL last-chance (in case IG re-opens guests) then fail fast with sentinel `ERR_SESSION_REQUIRED` (was: ~90s of dead-endpoint timeouts + misleading generic message). `withSessionRetry` auto-busts the memoized `ig:csrf` (new `cacheDelete` in cache.ts) and retries once on `login_required`/`challenge_required`/`checkpoint_*` (IG rotates csrftoken and rejects stale sessions often — one refresh-retry heals most). Highlights (`/stories/highlights/{id}` → username==='highlights') → honest `ERR_HIGHLIGHTS_UNSUPPORTED`. Photo stories (media_type 1) now download as **images**: new `isImageOnlyMedia` + API route serves `image/jpeg` + `tiksavehub-story.jpg` (was: "no downloadable media" for photo stories). **`src/lib/stream.ts` fix:** streamed responses now use the CALLER's `opts.contentType` first (was: upstream `Content-Type` overrode it — photo stories were served as video/mp4 when the CDN said so).
- **API (`src/pages/api/instagram-download.ts`):** sentinel-first error mapping — `instagram_story_expired` → "story disappears 24 hours…", highlights → clear not-supported copy, `instagram_session_required` → "protected content… server limitation" + console.error with the EXACT server fix (IG_SESSIONID missing), `login_required`/session_missing → transient copy + log (session rejected, refresh cookies). Cache-hit download path honors `video.isPhoto` too (same image/video split as fresh path).
- **Docs:** `.env.example` + `.env.production.example` now lead with: stories REQUIRE IG_SESSIONID/IG_DS_USER_ID/IG_CSRF_TOKEN (burner account recommended) + exact DevTools cookie-copy steps + a verification curl. Story tool cannot work without them — this is the ONE thing the site owner must configure at launch.
- **Verified:** `Temp/ig-story-flow-test.mts` (mock private API: tray hit/expired/media-info fallback/login-retry/uid-cache/no-session sentinel) 9/9 PASS; `Temp/ig-api-e2e-test.mts` (route-level: video story meta, photo story isPhoto + image/jpeg stream, cached dl photo, highlight copy, no-session fast <8s) 18/18 PASS; live smoke: story URL → 500 in **6.1s** with honest copy (was ~90s + generic); fake reel → standard fallback chain error (no regression).
- Uncommitted from prior sessions (same worktree): all FB fixes listed below (still staged-able).

## Prior session: FB story segment dedupe + probe-suite cleanup

Completed & verified (astro check 0/0/0, build green, story-set-parse 19/19, story-parse 9/9, login-detect PASS):
- `storySegmentsFromJson` (`src/lib/facebook.ts`) dedupes story segments by **SD URL** (`seenSd` Map) + upgrades the existing segment in place with the hd URL/cover when a later copy carries one — a duplicate segment with a DIFFERENT hd URL used to escape URL-keyed dedupe (best rendition wins, never duplicates). Probe fixture had 4 media items (video1 + [photo1, video2] + photo2) and its `/21` counter was hardcoded — both fixed in `Temp/story-set-parse-check.mts` / `Temp/story-parse-check.mts`; `check()` signature relaxed (`boolean|string|null|undefined`) because Temp files are type-checked by astro.
- Uncommitted from prior session (same worktree): FB story login-wall honest-error fixes (FIX 1-3), FB video pipeline parallelization, premium HD tile styling.

## Prior session: FB story login-wall root cause — honest error fix

Completed & verified (astro check 0/0/0, node probe tests PASS, live API smoke on dev IP):
- **User re-reported the same story error with a REAL fresh link** (`stories/122106409004612377/UzpfSVNDOjEwMDIzMzYzNjI3NzMwODE=/?view_single=1`). Token → `S:_ISC:1002336362773081`; owner uid `122106409004612377` = 18-digit page/restricted account. **ROOT CAUSE vs. assumption:** every known endpoint is **login-walled even from UNFLAGGED IPs** (verified via r.jina.ai reader proxy = clean-IP viewer): `story.php?story_fbid=…&id=…` serves a **noscript `<meta http-equiv=refresh content="0; URL=/login/?next=…">`** (the page IS a login redirect shell, 319-436KB); owner profile page, `stories.php?profile_id=`, mbasic/touch variants, embed plugin ("Video Unavailable… you don't have permission to view") and yt-dlp (login redirect) ALL require auth. This SUPERSEDES the earlier note "production IPs serve the story.php permalink page with video JSON" — story.php only serves media JSON for stories of **public personal profiles**; page/restricted-account stories go behind login. FB_ERR.NO_MEDIA was masking it as "expires after 24 hours".
- **FIX 1 (`src/lib/facebook.ts` `detectUnavailable`):** now also detects the **noscript login-redirect markers** (`url=/login/?next=`, `you must log in`, `log into facebook`, `action="/login`, password input) → throws `FB_ERR.LOGIN_REQUIRED` (was: only `/login` finalUrl match). Verified on REAL captured unflagged-IP page (jina HTML of story.php for this story): verdict now LOGIN_REQUIRED ✓; public video page (`videos/10153231379946729/` via jina) has NO login markers ✓ (no false positive).
- **FIX 2 (`src/lib/facebook.ts` `fetchFacebookStory`):** variant errors now carry `[code]` prefixes; if ANY variant code is LOGIN_REQUIRED → throw `FB_ERR.LOGIN_REQUIRED` (was: always collapsed to NO_MEDIA regardless of the true cause).
- **FIX 3 (`src/pages/api/facebook.ts` `storyMessageFor`):** new LOGIN_REQUIRED branch → honest copy: "This story requires a Facebook login to view (private or restricted account). Please try another public story link." (was: misleading "expires after 24 hours or may be private").
- **Verify:** `astro check` 0/0/0; 9-case parse probe: user raw link / `%3D`-encoded / no-token `/stories/` + `stories.php?profile_id` + `story.php` + `permalink.php` permalinks → ALL `linkType:'story'` with media id `1002336362773081` ✓ (video.php→`video_page`, share/r→`reel`, share/p→`photo` regressions = valid types, expected). Live dev-IP smoke: POST user's link (mode story) → 500 story-copy message KEPT old copy because dev IP shells the page before login markers appear (917B shell, no markers) — the login detection only fires on real production-style pages; the message fix itself verified via node probe on the captured real page. On production, this story will now produce the honest login-required message.
- IMPORTANT for future: this story type (page/restricted-account stories) is NOT anonymous-downloadable at all — no endpoint/fix can get it without a logged-in session (cookies). If the user reports it again with a NEW link, first check whether the OWNER is a public personal profile; story downloaders that handle restricted content do it client-side with logged-in browser sessions only.
- Uncommitted from prior session (same worktree): FB video pipeline parallelization (10.6s→8.2s flagged-IP), premium HD tile styling on FacebookDownloadForm (gradient border + sheen + HD badge chip).

Completed & verified (astro check 0/0/0, live API smoke on dev IP):
- **User: "video processing ko jitna fast kar sakte ho kar do".** Probe first (`fb-page-embed-timing.cjs` in Temp): page fetch = **10.5s** (200, 362KB throttled shell — the serial gatekeeper), embed = 2.7s (shell, no hd_src), embed 2nd = 0.8s. FIXES in `src/lib/facebook.ts`:
  - `fetchPage` now **bounded-read** via `readBoundedText` (cap `MAX_VIDEO_HTML_BYTES = 2.5MB`) + `VIDEO_PAGE_TIMEOUT_MS = 8s` (was full `resp.text()` with 25s timeout — on flagged IPs the page stall alone cost 10.5s and always returned a media-less shell anyway; healthy IPs deliver the full page in 1-2s so the cap never binds).
  - `fetchFacebookMedia` now **grace-starts the expensive sources in parallel with the page**: embed (+desktop-meta probe) after 2s, yt-dlp after 2.5s, via new `startWhenPageDelays(delayMs, task)` helper (page-gate resolves → cancels timer if page already yielded media; runs the task immediately if the page finished shelled; page throw (private/404/login) → never starts extras). Priority page > embed > yt-dlp. Healthy IPs: page wins in ~1-2s, embed/yt-dlp never start (same behavior/cost as before).
  - **Verified live on flagged dev IP:** fresh POST share/r → **8.2s** (was 10.6s; now bounded by yt-dlp's own ~5.7s runtime which starts at 2.5s — further gains would need a faster extractor, page cap is no longer the bottleneck). yt-dlp path won (title "7M views · 146K reactions | #fyp | Reetu Chaudhary", dur 16.866, hdplay mp4, cover populated). **Regressions PASS:** `videos/10153231379946729/` → page path wins (title "How to Share With Just Friends", dur 74, hd mp4; cover still empty on this IP — documented page-path limitation); `share/p/14jou2QjmXc/` → 5/5 photos, full-res `v51.82787-15` CDN URLs, titles intact. API smoke shape notes: POST video payload = `{video:{title,duration,hdPlay,sdPlay,cover,playCount,…}}`; photo payload = `{photo:{photos:[{photoUrl,altUrl,cover,title}],photoCount,…}}`.
- NOTE: earlier sessions' FB NO_MEDIA fix (`best[protocol!=m3u8][acodec!=none]/best[protocol!=m3u8]/best` — commas inside filter brackets are INVALID yt-dlp syntax), `toFacebookMedia` silent-DASH fix (hd/sd progressive preferred, sd→hd fallback), cover fix (`cover: thumbnail`), yt-dlp auto-update (`deploy/yt-dlp-update.{service,timer}` + setup-server.sh step 3b), TikTok `fetchTikTokItemDetail` quality probe, and the cross-site cover audit — all still in place (same worktree, uncommitted).

Completed & verified (astro check 0/0/0, build green, live API smoke on dev IP):
- **User bug report: "This video does not contain any downloadable media." (FB_ERR.NO_MEDIA, 500 on POST /api/facebook).** Repro: `web.facebook.com/share/r/1999ebjPbt` mode video — worked in earlier sessions, dying now. Probe: page fetch = 400 shell (len 1542, no media keys), embed = 200 len 49,137 but NO hd_src/sd_src keys and NO title on this flagged IP; direct yt-dlp CLI on the same URL worked (full JSON: "7M views · 146K reactions | #fyp | Reetu Chaudhary", id 28060072830317328, dur 16.866). 
  - **ROOT CAUSE:** my previous session's format-selector edit `best[protocol!=m3u8,acodec!=none]` is **INVALID yt-dlp syntax** — commas are not allowed inside filter brackets (they're selector separators). Every yt-dlp fallback call died with `SyntaxError: Invalid filter specification` — and the yt-dlp path is the ONLY working path on flagged IPs (page+embed both shelled). FIX in `src/lib/ytdlp.ts` line 120: `best[protocol!=m3u8][acodec!=none]/best[protocol!=m3u8]/best` (separate brackets). Verified via CLI with the app's exact args: parses, selects format_id `hd`.
  - **SECOND ISSUE found live (silent/audio-only picks in `toFacebookMedia`):** FB's yt-dlp JSON reports the progressive `hd`/`sd` formats with acodec/vcodec as **null** (not "none") and NO height; the DASH formats have real heights (720x1280/1080x1920) but `acodec:"none"` (video-only), plus one DASH audio m4a. Old picks → hdUrl = silent AV1 DASH 720p (would download fine but NO audio; AV1 also unplayable in many players), sdUrl = the m4a audio-only file. FIX: `toFacebookMedia` now prefers `format_id === 'hd'/'sd'` progressives first (they ARE the combined A+V files — verified 2,670,609B file contains BOTH `mp4a` and `avc1` sample entries), then combined non-m3u8 (acodec≠none && vcodec≠none), then old height-based fallbacks; sd falls back to hd (never worse, matches pickUrl behavior).
- **Verified live (full node kill + cache clear + restart — server also got Ctrl+C'd once by an aborted tool call; restart pattern unchanged):** fresh POST share/r → 200, hdplay = 720p progressive `tag=dash_h264-basic-gen2_720p` @1.26Mbps, sdplay = 360p `tag=sve_sd` (this variant), title/duration(16.866)/thumbnail avatar/play_count 2,499,701 all present; `dl=hd` → **200, 2,670,609B video/mp4, H.264 + audio (binary-scanned: mp4a ✓ avc1 ✓ av01 ✗)**; `dl=sd` → 200 (fresh-fetch variant lacked the sd rendition → sdUrl fell back to hd — never worse). **Regressions PASS:** `facebook/videos/10153231379946729/` → page-JSON path still wins (title "How to Share With Just Friends", dur 74, hd `tag=hd` 1280px); `share/p/14jou2QjmXc/` → 5/5 photos, all `stp=dst-jpg_tt6&cstp=mx1179x1572` no ctp, altUrl ctp-fallback intact.
- NOTE: the POST title shows `·`/emoji as mojibake in PowerShell console display only (console codepage — the API payload is correct UTF-8; `PYTHONIOENCODING=utf-8` already set in runYtDlp env).
- **User: "thumbnail show nai hota"** — cover empty in the result card. ROOT CAUSE: on flagged IPs the yt-dlp path (`toFacebookMedia`) extracted the thumbnail (it was reaching `author.avatar`) but NEVER returned a `cover` field → API `cover: undefined` → frontend icon placeholder. FIX: `toFacebookMedia` (src/lib/ytdlp.ts) returns `cover: thumbnail` alongside `thumbnail`. Verified live: fresh POST share/r → cover populated (scontent t15.5256-10 URL, downloads 57,531B JPEG 200); frontend already renders it with `referrerpolicy="no-referrer"` + graceful `onerror`. NOTE: the regression video `videos/10153231379946729/` still has EMPTY cover on THIS dev IP — its page-JSON path wins and that IP's page has no preferred_thumbnail/og:image (documented limitation; production IPs get preferred_thumbnail via page path). Also NOTE for future testing: stale video rows in `data/cache/media-cache.json` (keys `media:fb:video:*`) serve pre-fix shapes — clear script only removes photo keys; video keys need manual removal (node one-liner) or full cache wipe before re-testing payload changes.
- **Cross-site cover audit ("puri website pr kr do"):** checked every tool's cover flow + verified live on dev IP — TikTok/IG/MP3s were ALREADY correct (no code change needed): TikTok `toTikTokData` returns `cover: json.thumbnail`, IG is covered via `getThumbnailUrl` reading `image_versions2` (both native + yt-dlp paths), TikTok-MP3 + FB-MP3 pages use `video.cover` (now populated on FB since the fix). Live: GET /api/download (TikTok `@krisan.geline/video/7632685614411844881`) → cover URL ✓; GET /api/instagram-download (`reel/DbXt_hjz53a/`) → cover URL ✓; /api/download-mp3 + FB mode audio → cover URL ✓. FB was the ONLY tool missing it. NOTE: TikTok/IG API routes are GET-only (POST → 404) — testing pattern differs from FB.
- **User: "tiktok video quality ko max kr do".** Probed the real ceiling first: tikwm (`hd=1`) returns a SINGLE progressive file — `size == hd_size` (1,982,045B), parsed via MP4 box walk (container whitelist + stsd/dref/meta skip-offsets): **576x1024** = TikTok's LOWEST rendition on this flagged IP. yt-dlp TikTok extraction = blocked on this IP ("Unexpected response from webpage request"), TikTok `/api/item/detail/?itemId=&aid=1988` = 200 with EMPTY body (729ms) on this IP. So no live 1080p verification possible here — real upgrade ships via the new probe.
- **FIX (`src/lib/tiktok.ts`):** new `fetchTikTokItemDetail(itemId)` — TikTok's own item-detail API, 8s abort, **never throws** (null on challenge/block/parse/empty). `fetchTikTokMetaWithFallback` runs it after normalize and REPLACES `hdplay` with `downloadAddr || playAddr` when present (typically 720p-1080p no-watermark; also fills new optional `meta.quality {width,height}`). download.ts untouched — `pickStreamCandidates(hd)` already tries hdplay first. On flagged IPs item-detail nulls → behavior identical to before (+~0.7s). Verified live on dev IP: meta fetch + dl=hd → 200, 1,982,045B MP4 ftyp ✓, no errors; item-detail probe timing 729ms/empty on this IP. On unflagged production IPs the item-detail URL should pop the higher rendition — re-verify with a real link on a clean IP after launch.

Completed & verified (astro check 0/0/0, build green, live API smoke on dev IP):
- **User: "facebook video ki quality best karo jitna kar sakte ho".** Probed the REAL ceiling first (node fetch, same headers as server): the test video's source is **1920x1080** (`original_width/height` in videoData) but FB serves progressive **hd = 1280px @580kbps** (`efg` tag `C3.1280.hd`, `bitrate=580000`); `dash_manifest` = **null** on this IP for this video; `bitrate=`/`tag=` query swaps are metadata-only (same ~5.4MB file); embed og:image/preferred_thumbnail/image.uri all absent (cover limitation stands, documented). No hidden bigger rendition exists to serve — our path was never capping (no ctp-style param on video URLs).
- **FIXES (`src/lib/facebook.ts`):** `extractMediaFromPageHtml` hd/sd now `playable_url_quality_hd → browser_native_hd_url → hd_src_no_ratelimit → hd_src` (page variants rotate which keys exist per request — probe saw a variant with ONLY `browser_native_*`); `extractMediaFromEmbedHtml` prefers `hd_src_no_ratelimit`/`sd_src_no_ratelimit` (same rendition, unthrottled delivery) over `hd_src`/`sd_src`, og:video last-resort unchanged.
- **FIX (`src/lib/ytdlp.ts`):** fallback selector `best[protocol!=m3u8]` → `best[protocol!=m3u8,acodec!=none]/best[protocol!=m3u8]/best` — never picks a silent video-only DASH format when a combined (with-audio) format exists (FB dash-hd is video-only); `toFacebookMedia` already prefers height>=720.
- **Verified live (full node kill + cache clear + restart):** fresh POST `facebook/videos/10153231379946729/` → success, uncached, dur 74, HD URL = 1280px progressive (`tag=hd`); `dl=hd` → **200, 5,388,007B video/mp4** full file; `dl=sd` → 200 same (FB omitted sd this variant → pickUrl falls back to HD — SD button is never worse). On unflagged production IPs FB's own `playable_url_quality_hd` is often 1080p — served as-is, uncapped.
- NOTE: prior photo session's ctp-strip etc. all still in place (same worktree, still uncommitted).

Completed & verified (astro check 0/0/0, build green, live API smoke on dev IP):
- **User: "inki quality ko improve karo"** — downloads capped at s590 (reader siblings) / p600 (og:image). Probe matrix on real signed URLs from cache (`curl` variants): `ctp=s590x590` → `s2048x2048` = **400** (raise locked), `cstp=mx…` change / `_n.jpg`→`_o.jpg` = **403** (signed) — but **removing `ctp=` entirely = HTTP 200 1179x1572 original (~188KB vs 35KB)** on BOTH URL types (reader `stp=dst-jpg_tt6` AND og:image `cp0_…tt6`). FB signs stp/cstp/path tokens but NOT the `ctp` size cap — dropping it serves the natural-size file (cstp=mx1179x1572 = the file's real dims).
- **FIX in `src/lib/facebook.ts` `promotePhotoUrl`:** strip `([?&])ctp=[sp]\d{1,5}x\d{1,5}` (+ collapse `&&{2,}`). No score/dedupe regression: `photoQualityScore` counts cstp (1572) so stripped URLs tie with capped raws (isThumbOnly still checks RAW alt — untouched); promotion applies at all 3 creation sites (HTML extraction, reader recovery, sibling photo.php). Signed URLs that still reject keep the `altUrl` fallback path.
- **Verified live (cache cleared + full node kill + restart — IMPORTANT: earlier "stale cache" ghost was a still-running old node holding port 3000 in memory; kill ALL `Get-Process node`):** POST share/p → 5 photos, all promoted URLs `stp=dst-jpg_tt6&cstp=mx1179x1572` NO ctp, no `&&`; `dl=photo` → **1179x1572, 183,648B** (was 600x800/41KB); `dl=zip` → **5/5 entries 172-280KB full-res JPEGs (~818KB zip)** (first zip run dropped photo-05 — transient CDN 32,768B stub; direct re-curl of same URL = 280KB 1179x1572; second zip = 5/5 — transient, no code issue); per-file direct curl 200: 183/188/172/273/280KB all 1179x1572.
- Known trade-off: none beyond the ~3-20s reader path on flagged IPs (unchanged). On unflagged IPs the JSON `"image":{"uri":…}` entries (untouched full-res) now also get ctp-stripped — same or better.
- Prod note: `ctp` strip is safe for all future fbcdn URLs — worst case a signed-`ctp` URL 400s → `altUrl` fallback delivers the capped original (no photo loss).

Completed & verified (astro check 0/0/0, build green, 31-case regex test ALL PASS, live API smoke on dev IP):
- **FB post permalinks now parse as photos.** `src/lib/facebook-url.ts`: new `PROFILE_POST_RE` (`/{user}/(posts|permalink)/{token}`) + `GROUP_POST_RE` (`/groups/{gid}/(posts|permalink)/{token}`) — token 8-80 chars (legacy numeric or modern `pfbid…`), `linkType:'photo'`, `photoId: token`, sanitizedUrl = `https://{host}{pathname}/` (host preserved for the flagged-IP web. preference). This is what FB's "Copy link" produces for photo posts; the post page HTML carries og:image + `"image":{"uri":…}` JSON so the existing extractor reads it like any photo page. Client-side `URL_PATTERN` was already generic (any facebook.com path) — only the error-message strings were updated (form + api + fallback invalid()).
- **Reader-recovery gap fixed in `src/lib/facebook.ts`:** `isReaderRecoverableUrl` covered `share/p/{code}` + profile permalinks but NOT group posts (docstring claimed it did — parse accepted `groups/{gid}/posts|permalink` but a flagged IP would get zero photos with no reader fallback). Added the `/groups/{gid}/(posts|permalink)/{token}` alternation.
- **Regex tested via Node 24 native TS import** (no tsx needed): 31 cases ALL PASS — 6 acceptance (profile posts pfbid/numeric, profile permalink, group posts pfbid, group permalink, underscore profile), 3 still-photo regressions (share/p, photo.php, photos path), 10 not-photo (videos/reel/share-r/share-v/stories/watch/fb.watch/permalink.php legacy/posts-no-token/posts-short-token/groups-bare), 8 reader-gate cases (6 recoverable incl. both group shapes / 3 skipped video+photos+photo.php).
- **Live smoke on dev IP (restarted server with the new code):** share/p regression → 200 `photoCount:5` (photo-01 = cp0_…p600 og:image-style, 02-05 = reader-recovered `dst-jpg_tt6` s590 JPEGs — cached copy served at 59ms on second call); fabricated profile-posts URL + group-posts URL → both parse + route to photo pipeline, ~33s graceful 500 `{"success":false,"error":"Could not load this Facebook photo. Check the link or try another public photo."}` (no 422 parse rejection, no server crash). NOTE: no REAL public post-permalink token was available to smoke a successful full-recovery run — on flagged IPs real posts URLs should recover via the reader just like share/p (same s590 sign-and-deliver path); re-verify with a real link when one is handy (cache-clear + kill node + `npm run dev`).
- Uncommitted from prior session (same worktree, still staged-able): the referrerpolicy/thumb-failed fix + q{quality}_s{size} stp promotion (see next section) + SEO FAQ rewrites across all tool pages.

Completed & verified (astro check 0/0/0, build green, regex tests ALL PASS, live API smoke on dev IP):
- **User bug report: "2nd/3rd photos broken/thumbnail"** (carousel `share/p/14jou2QjmXc/` — 5 photos). Two gaps found + fixed:
  - **Frontend (Issue 1 — broken grid/preview images):** `FacebookDownloadForm.astro` client `<img>` tags hotlinked FB CDN URLs with NO referrer policy — the browser sent `tiksavehub.com` as Referer and FB's CDN refused some sibling renditions (photo 1 worked, 2-5 broke). FIX: added `referrerpolicy="no-referrer"` to all 3 client-rendered FB imgs (photo grid item, photo preview, video cover) + `onerror` hides failed imgs via new `.result-thumb-wrap.thumb-failed` CSS (graceful placeholder, no broken-icon). Verified: 3 `referrerpolicy` refs in the dev-served script module.
  - **Backend (Issue 2 — 320px sibling extraction):** `promotePhotoUrl` only matched `stp=dst-jpg_s{N}x{N}` — real pages also ship **`stp=dst-jpg_q80_s320x320`** (quality-prefixed) which stayed 320px. FIX: patterns now `stp=dst-jpg(?:_q\d+)?_s\d+` → `dst-jpg_p2048x2048` (jpg + webp), plus new path-token bump `/(\/s\d+...\/)/ → /s2048x2048/`. `stp=dst-jpg_tt6` (no size token) still untouched — downloads directly. Regex-tested 7 real URL shapes: ALL PASS.
- **Live smoke:** POST `share/p/14jou2QjmXc/` on fully flagged dev IP → 200 `photoCount:5` fresh (no cache): photo-1 = `cp0_dst-jpg_e15_fr_q65_tt6` og:image-style, photos 2-5 = `stp=dst-jpg_tt6` reader-recovered full-size (443x590, verified 200 image/jpeg in earlier session). No `s261x260`/`s320x320` thumbs anywhere. Existing sibling layers (JSON `"image":{"uri":…}` extraction, `photo.php?fbid=` rounds, reader-proxy fallback, `altUrl` download fallback) unchanged.
- Cache/restart procedure for re-testing unchanged: `scripts/clear-fb-photo-cache.cjs` + kill node + `npm run dev`.

Completed & verified (astro check 0/0/0, build green, live API smoke):
- **User re-reported "phir se single photo download"** on `share/p/14jou2QjmXc/` — the ZIP already had 5 entries but 4 were tiny signed thumbs (~4-8KB webp) on this IP: from the user's POV it's still "1 photo". Re-verified EVERY source on the dev IP one more time (share page web/www/m × UAs, touch story, photo.php all hosts, stp removal/rewrite → 403 — signed/locked, bare path → 403). **Breakthrough:** fetched the share page through the public reader proxy **`r.jina.ai/{url}`** (unflagged IP) — it returns ALL 5 sibling photos at **`stp=dst-jpg_tt6&cstp=mx1179x1572&ctp=s590x590`** (443x590 JPEG, 32-52KB), and those signed URLs **download from ANY IP** (FB CDN signatures are IP-independent; verified 200 image/jpeg from dev IP). `photo.php` via jina = login wall even for its IPs; touch story/m. share via jina = same 5 at s590; web story = nothing. s590 ≈ the p600 og:image standard competitors use → good enough.
- **FIXES in `src/lib/facebook.ts`:**
  - New `fetchSiblingsViaReader(shareUrl, wantIds|null, found)`: fetches `https://r.jina.ai/{shareUrl}` (Accept: text/plain, 20s timeout), regex-parses plain-text markdown for `https://…scontent…` URLs, matches by `photoIdFromUrl` (or accepts all when `wantIds=null`), quality-gates > `MIN_FULL_PHOTO_SCORE`, promotes, 1 quick retry on failure. Never throws (thumbs kept).
  - `fetchFacebookPhotoSet`: recovery triggers when `bestCandidates.length === 0` (fully shelled IP — reader recovers the WHOLE set) OR `count === 1 && sawTruncated` (throttled partial head lost the siblings — reader recovers them; clean single-photo pages skip the proxy entirely). Reader entries merge into candidates by **CDN path** (`cdnPathOf` — new helper; dedupes by pathname not full URL, since the same file appears on different CDN hosts like `flhe2-2` vs `sea5-1`).
  - **CRITICAL thumb-detection fix:** sibling upgrade now checks the RAW rendition (`c.alt`), not the promoted URL — a signed thumb promoted to `dst-jpg_p2048x2048` *looks* full-size (score 2058 > 320) but the p2048 403s at download and delivers the 261px thumb anyway. `thumbIds` = `isThumbOnly(c.alt)`; recovered entries now replace **unconditionally** (they're guaranteed ≥ thumb).
  - `readBoundedText`/`fetchPhotoPage` return `{html, truncated}` (truncated = maxBytes hit or mid-body stall) → `bestFrom` tracks `sawTruncated`; new `allTimedOut` check skips the native retry round when every attempt timed out (throttled IP — retrying immediately is wasted 15s; go straight to reader).
  - `fetchSiblingPhotosFull(ids, shareUrl?)`: after native photo.php rounds, round 1 returning NOTHING skips rounds 2-3 (walls) and goes straight to the reader for remaining ids (35s → ~15s on dev IP).
  - `promotePhotoUrl` leaves `stp=dst-jpg_tt6` (no size token) untouched → reader URLs download directly with no 403 dance (`altUrl` stays undefined → single request).
- **Verified live on dev IP:** POST user's link → 200 `photoCount:5` — photo-01 = p600 og:image, photos 02-05 = **s590x590 reader-recovered** (was: 4 tiny webp thumbs); ZIP → **212,247 bytes, 5/5 real JPEGs** (photo-01.jpg 40,962B 600x800; photo-02..05.jpg 32-52KB **443x590** — verified via System.Drawing); single `dl=photo` → 40,962B image/jpeg; video `dl=sd` regression OK (5.4MB video/mp4). Cache-clear + restart procedure unchanged (`scripts/clear-fb-photo-cache.cjs` added to repo — BOM-safe node script that deletes `media:fb:photo*` keys from `data/cache/media-cache.json`; then kill node + `npm run dev`).
- Known trade-off: reader proxy path adds ~3-20s on fully flagged IPs only (production/unflagged IPs never hit it — native JSON already full-res); jina keyless rate limits are handled by the 1-retry + thumbs-kept degradation.


Completed & verified (astro check 0/0/0, build green, live API smoke):
- **Multi-photo ZIP gave 1 real photo + 4 TINY sibling thumbs (user bug report: "sirf single photo download kr raha hai" for `share/p/14jou2QjmXc/`).** Root cause found live: the share/story shells on this (and apparently user's) IP serve each sibling ONLY as a signed **quad thumbnail** (`stp=dst-webp_q70_s261x260/s173x172`, ~4-8KB webp) — no full-size URI anywhere, and `promotePhotoUrl` didn't touch `dst-webp` tokens, so the ZIP shipped the thumbs as-is (looked like "1 photo"). Verified every alternative source on the dev IP: `photo.php?fbid={id}` (4 hosts × UAs) = login-wall shells or 880B shells with NO og:image; `story.php?story_fbid={postId}` (all hosts) = no image data; `mbasic` = login redirect; `plugins/post.php` = JS-only shell; CDN `stp=` rewrites of the signed thumbs → **403** (locked); `photo/?fbid=` = 880B shell. Competitor check (Google: FDown/GetVidFB/Apify/scripts): the standard technique = fetch each sibling's **`photo.php?fbid={photoId}`** page and take ITS og:image (full-res) — photo IDs are the FIRST numeric segment of the CDN path (`{photoId}_{fbid}_{…}_n.jpg`).
- **FIXES in `src/lib/facebook.ts`:**
  - `extractPhotosFromHtml` → returns `PhotoCandidate[]` (`{url, alt}` = promoted + raw original). Dedupe by URL path now keeps the **best rendition** per photo via new `photoQualityScore(u)` (jpg/png +10 vs webp +1; adds `s/p` stp dims, `ctp=p{w}x{h}`, `cstp=mx{w}x{h}`, `/p{w}x{h}/` path dims; an unsigned `dst-jpg` with NO size token = ORIGINAL → +2000). So og:image (p600/2048) always beats the touch-page quad thumb of the same photo.
  - `promotePhotoUrl` now also upgrades `stp=dst-webp_q70_s{N}x{N}` → `stp=dst-jpg_p2048x2048` (was jpg-only). Downloaders retry the raw `alt` URL when the promoted URL 403s (signed/locked) — **no photo is ever lost to a promotion**.
  - New `photoIdFromUrl(u)` (first numeric CDN segment) + `isThumbOnly(u)` (quality < `MIN_FULL_PHOTO_SCORE = 320`).
  - New `fetchSiblingPhotosFull(ids)`: per-photo `photo.php?fbid={id}` matrix (web. iPhone → www. desktop → m. iPhone, 6s timeout each) in 3 parallel rounds, only for ids still missing; takes the page's og:image (rejects login/static/facebook.com URLs), promotes it, replaces the thumb in the set. `fetchFacebookPhotoSet` triggers this only when thumb-only siblings exist (flagged IPs: all rounds fail fast → thumbs kept; unflagged IPs: round 1 yields the full og:image).
  - `FacebookPhoto` gained `altUrl?: string`; `fetchFacebookPhoto` passes it through. `fetchPhotoBuffer(url, altUrl?)` falls back to `altUrl` on non-ok. `fetchFacebookPhotosAsFiles` now takes `Array<{url, alt?}>`.
  - `readBoundedText` no longer discards buffered head HTML when FB stalls/throttles mid-body (catch → return partial) — a throttled page still yields its og:image instead of a wasted 15s timeout.
- **API (`src/pages/api/facebook.ts`):** POST photo payload + GET zip cache payload now carry `altUrl` per photo; GET `dl=zip` passes `{url, alt}` objects (both cache-hit and fresh-fetch paths); single `dl=photo` streams promoted URL and falls back to `altUrl` on upstream failure.
- **Verified live on dev IP:** POST user's link → 200 `photoCount:5` in ~15s (FB throttled the IP mid-session; normal ~2-5s), photos 2-5 promoted to `dst-jpg_p2048x2048` with `altUrl` set; ZIP → 64,710 bytes, **5 entries** (photo-01.jpg 40,962B FF D8 FF; photo-02..05.webp RIFF 4-7.7KB — p2048 403'd on dev, alt fallback downloaded the original thumbs, no entry lost); single `dl=photo` → 40,962B JPEG; video `dl=sd` regression OK (1.18MB ftyp). Extractor unit-tested via tsx against 6 REAL saved FB pages: share-iPhone full (5), throttled partial head (5), touch story (5), photo.php Urdu login wall (0), story web shell (0), plugin shell (0) — ALL PASS. `extractPhotosFromHtml` is now exported for future tests.
- NOTE: dev IP got hard-throttled mid-session by the request barrage (share page stalled at 113-167KB, curl 30s timeouts) — cooldown restores normal behavior; not a code issue. First POST after cache-clear can hit a stale `memoSWR`/disk cache → when re-testing: kill node + delete `media:fb:photo*` keys from `data/cache/media-cache.json` (node script, BOM-safe) + restart dev server.
- On unflagged production IPs the story JSON already yields full-res sibling URIs (no per-photo fetches needed); the per-photo fallback covers partial-flag cases. ~~The 4 sibling thumbs on this dev IP stay small because FB signs them~~ — **superseded:** the reader-proxy fallback (`r.jina.ai`, see WIP at top) now recovers s590 full-size siblings even on fully flagged IPs.
- **FB share-link formats fixed (user bug report).** `src/lib/facebook-url.ts`: added `SHARE_REEL_RE = /^\/share\/r\/([A-Za-z0-9_-]{4,20})\/?$/` + `SHARE_VIDEO_RE = /^\/share\/v\/([A-Za-z0-9_-]{4,20})\/?$/` — `web.facebook.com/share/r/{code}` (Reels) and `/share/v/{code}` (videos) now parse as valid videos (`linkType:'reel'/'profile_video'`, `shortCode: code` → cacheKeyOf, `sanitizedUrl` = the share URL itself since FB redirects it client/server-side; works on web./m./mbasic./touch. hosts which were already in FB_HOSTS). Extraction needs no changes — embed plugin (`/plugins/video.php?href=` + `videoData`), page-fetch and yt-dlp all accept share URLs. Verified live: user's link `web.facebook.com/share/r/1999ebjPbt` → POST 200 with title/duration/HD+SD URLs, `dl=hd`/`dl=sd` stream 200 video/mp4 (~665KB). Cover still empty on dev IP (documented limitation).
- **Photo fetch speedup (user report: "process takes too long").** `src/lib/facebook.ts`: photo path no longer waits sequentially on two full page downloads — (1) `fetchPhotoPage` now reads the response **bounded-stream** (`readBoundedText`, caps at 2.5MB, cancels the body reader once enough HTML is in — og:image lives in `<head>`, JSON blobs shortly after) with a 12s timeout (was 20s + full `resp.text()` of multi-MB pages); (2) desktop + mobile pages now fetch **in parallel** via `Promise.allSettled`, first page yielding a usable image wins — worst case (flagged dev IP) went from ~40s of sequential timeouts to a hard ~2s. Verified: photo POST on dev IP now 1.9s (error is the documented shell limitation).
- **Per-tool result tiles (user requests):** each FB tool's result card now shows ONLY its own download button(s) — `FacebookDownloadForm` JS: `tiles = isMp3Mode ? [audio] : [hd, sd]` (verified in compiled bundle `f?[H]:[F,j]`). MP3 page = audio tile (FAQ `mp3-video-with` → points to video/reels pages); video page = HD + SD (FAQ `dl-reels`/`dl-mp3`/`dl-live`, hero sub, "How the Facebook video downloader works", "Extract Facebook audio as MP3", "What quality and formats", live replays, "Where do downloaded videos go", step 03 all updated to drop the Audio tile and link `/facebook-to-mp3`); reels page = HD + SD (FAQ `reels-audio`, "Save Reel audio as MP3 too", step 03 → `/facebook-to-mp3`); story page = HD + SD (article audio mentions + step 03 → `/facebook-to-mp3`); photo page = photo tile only. NO page shows all three tiles anymore.
- KNOWN DEV-IP LIMITATION (same as video cover): this dev machine IP gets shelled FB pages — photo.php desktop = 200 shell (~440KB) with NO og:image, NO `"image":{"uri":…}`, only reaction-emoji scontent assets (deliberately skipped); mobile = 888B error shell; post plugin = 400; graph oEmbed = html w/o thumbnail. On unflagged IPs, desktop photo.php pages carry og:image → extractor works (standard pattern used by all FB photo downloaders). Do NOT chase a dev-IP workaround.

Key implementation facts (IMPORTANT for future work):
- Production env setup artifacts exist: `.env.production.example`, `scripts/setup-server.sh`, `deploy/tiksavehub.service`. Node standalone server does NOT auto-load `.env` files → env must come from the process (systemd `EnvironmentFile=/etc/tiksavehub/env`). Set `CACHE_FILE=/var/lib/tiksavehub/media-cache.json` (outside the deploy dir; `dist/` is wiped on redeploy). yt-dlp standalone binary at `/usr/local/bin/yt-dlp` (self-update `yt-dlp -U`). Site NOT launched yet — artifacts wait for launch day.
- **Auto-updating yt-dlp:** `deploy/yt-dlp-update.service` (oneshot `yt-dlp -U`, logs to `/var/log/tiksavehub/yt-dlp-update.log`) + `deploy/yt-dlp-update.timer` (daily, +6h random delay, `Persistent=true`). `scripts/setup-server.sh` step 3b installs both and `systemctl enable --now` the timer. Note: setup runs as root, so root self-update of `/usr/local/bin/yt-dlp` has no permission issue. Check status: `systemctl list-timers yt-dlp-update.timer`.
- Full Facebook downloader module built: `src/lib/facebook-url.ts`, `src/lib/facebook.ts`, `src/lib/ytdlp.ts` (facebook helpers), `src/pages/api/facebook.ts` (POST metadata + GET dl=hd|sd|audio with ffmpeg MP3), `src/components/FacebookDownloadForm.astro`, `src/pages/facebook-video-downloader.astro` (SEO landing + JSON-LD).
- Wired into `astro.config.mjs` (sitemap), `NavBar.astro`, `Footer.astro`, `Layout.astro`, `src/lib/pageSections.ts`.
- `astro check`: 0 errors/hints. `npm run build`: green. Live API smoke tests pass.

Key implementation facts (IMPORTANT for future work):
- Extraction order: page JSON (`playable_url_quality_hd`/`playable_url`) → embed plugin page `"videoData"[{hd_src,sd_src}]` (works even on FB login-wall/IP-flagged shells; direct progressive MP4s) → yt-dlp fallback.
- Duration on embed path decoded from base64url `efg` payload in CDN stream URLs (`durationFromEmbedConfig`, regex `&efg=([A-Za-z0-9_-]+)`, padded `atob` decode, `duration_s` field).
- CDN streams need `Referer: https://www.facebook.com/` to download (verified 200, video/mp4).
- Test video used everywhere: `https://www.facebook.com/facebook/videos/10153231379946729/` (dur 74s).
- Dev machine IP gets FB "Error" shell (HTTP 400, ~3.6KB) on **mobile** page fetch, but the **desktop** page fetch returns the FULL page (status 200, ~950KB) with real `<title>`/`og:title`/`meta description` — yet NO `preferred_thumbnail`, NO `og:image`, NO `playable_url*` keys on this IP. Production IPs get the full JSON via the mobile page (title/cover/counts via `preferred_thumbnail`).
- Embed plugin page (previous session): title/author now extracted from its markup — anchor `watch/?ref=embed_video">TITLE</a>` and `/watch/{page}/?ref=embed_video` link text; page/author avatar at s40x40 is size-bumped via `s40x40→s320x320` in the CDN `stp` param.
- `graph.facebook.com/v26.0/oembed_video?url=` works WITHOUT auth on public videos (returns title/author/date in the `html` field; NO thumbnail). Not currently used in code.
- Public metadata probe `fetchDesktopPageMeta(url)` in `facebook.ts` runs in parallel with `fetchEmbed` and fills `cover` (via `preferred_thumbnail`) + page name when the desktop page is served even though the mobile page shelled. Never throws.
- FB tool suite (built across sessions): 5 pages (`/facebook-video-downloader`, `/facebook-reels-downloader` = MAIN brand tab, `/facebook-story-downloader`, `/facebook-to-mp3`, `/facebook-photo-downloader`) sharing `FacebookDownloadForm` (mode prop + tabLinks incl. `main:true` Reels brand pill `fb-tab-main`/`fb-tab-pulse`) + 7 Fb* section components. NavBar FB links → reels (desktop `nav-link-fb` L37, mobile `mob-link-fb` L127).

Known limitation (documented, no action planned):
- On the embed fallback path with a fully flagged IP (like this dev machine), `cover` is still empty — no source exposes an og:image/thumbnail for such IPs (desktop page has no preferred_thumbnail; oEmbed has no thumbnail; the ambiguous `t15.5256-10` preloads are related-reels images and are deliberately NOT used). Result card now shows a graceful icon placeholder instead of a broken `<img>` when cover is empty.
