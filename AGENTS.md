
Always Use:
atro, tailwind-4-docs, web-design-guidelines these 3 skills for this project
DESIGN.md for this project design

## WIP Status (last session: FB Photo Downloader tool built — parse, extraction, API, form, page)

Completed & verified (astro check 0/0/0, build green, built HTML inspected + API smoke):
- **FB Photo Downloader built end-to-end.** `src/lib/facebook-url.ts`: `FacebookUrlParseResult` now has `photoId: string | null`; photo parsing added — `PHOTO_PAGE_PATH_RE = /^\/(photo\.php|photo)\/?$/` (+ fbid/photo/story_fbid/id query params), `PHOTO_PROFILE_RE` (`/{user}/photos/{id}`, album-prefixed `a.`/`p.` forms), `PHOTO_VIEW_FULL_RE` (`/photo/view_full_size/`). Photo results: `linkType:'photo'`, `isVideo:false`, `sanitizedUrl` → `photo.php?fbid={id}`. All other parse sites now include `photoId: null`.
- `src/lib/facebook.ts`: new `FacebookPhoto` interface + `fetchFacebookPhoto(url)` (memoSWR `fb:photo:{url}`, 12h TTL) + `extractPhotoFromHtml()` — tries desktop page then mobile (`www.`→`m.` rewrite), extracts in order: og:image meta → `"image":{"uri":…}` JSON blob → largest non-avatar scontent/fbcdn `<img>` (skips `s\d+x\d+`/emoji/avatar assets); `promote()` rewrites CDN size tokens (`stp=dst-jpg_s…`→`p2048x2048`, `/p\d+x\d+/`→`/p2048x2048/`) for max resolution. Errors via `coded()` with `FB_ERR` codes.
- `src/pages/api/facebook.ts`: POST body now accepts `mode` (`'photo'`); photo links route to photo extractor (cache `cacheHit/cacheWrite('facebook','fb',{photoId},'photo')`), response `{success,type:'facebook-photo',photo:{photoUrl,cover,title,author}}`; cross-validation — photo link on video tool → 422 "use the Facebook Photo Downloader", video link on photo tool (`mode:'photo'`) → 422 "That link is a Facebook video, not a photo". GET `dl=photo` streams the image (`streamFromUpstream`, `referer: https://www.facebook.com/`, `image/jpeg`, filename `tiksavehub-facebook-photo.jpg`). New `photoMessageFor(err)` for photo-specific user errors. `cacheKeyOf` now includes `parsed.photoId`.
- `src/components/FacebookDownloadForm.astro`: `mode` prop now `'video'|'reels'|'story'|'mp3'|'photo'`; 5th tab "Photo" (`/facebook-photo-downloader`, image icon); `photo` copy map (placeholder "Paste Facebook photo link"); photo result card = square 200px preview (`photo-preview` class) + single `dl-tile-fb dl-tile-prime` "Download Photo" button w/ "Full Resolution" badge; `startDownload('photo')` filename `.jpg`; POST body sends `{url, mode}`; photo-mode validation/error copy.
- `src/pages/facebook-photo-downloader.astro`: full SEO landing (unique title/desc/keywords/canonical/OG + BreadcrumbList + FAQPage + SoftwareApplication 4.8/8740), FbHeroSection mode="photo", Stats 2M+, 600+ word FbSeoArticle, steps/features/FAQ/CTA, FbInternalLinks incl. photo card.
- Wiring: `astro.config.mjs` seoPages + `src/lib/pageSections.ts` TOOL_PAGES got `/facebook-photo-downloader`. `FbInternalLinks.astro` got `'photo'` icon (image icon) + card added to video (custom grid)/reels/story/mp3 pages. NavBar untouched (still → reels).
- Verified served HTML: photo page 200 with `data-mode="photo"`, Photo tab `fb-tab-active`, Video/Reels/Story/Audio tabs intact on all 5 pages, Reels keeps `fb-tab-main` pill, homepage nav still only `/facebook-reels-downloader` (0 old links). API smoke: photo URL → 500 photo-copy error on THIS dev IP (see limitation below); reel-on-photo-tool → 422 with correct message.
- **FB share-link formats fixed (user bug report).** `src/lib/facebook-url.ts`: added `SHARE_REEL_RE = /^\/share\/r\/([A-Za-z0-9_-]{4,20})\/?$/` + `SHARE_VIDEO_RE = /^\/share\/v\/([A-Za-z0-9_-]{4,20})\/?$/` — `web.facebook.com/share/r/{code}` (Reels) and `/share/v/{code}` (videos) now parse as valid videos (`linkType:'reel'/'profile_video'`, `shortCode: code` → cacheKeyOf, `sanitizedUrl` = the share URL itself since FB redirects it client/server-side; works on web./m./mbasic./touch. hosts which were already in FB_HOSTS). Extraction needs no changes — embed plugin (`/plugins/video.php?href=` + `videoData`), page-fetch and yt-dlp all accept share URLs. Verified live: user's link `web.facebook.com/share/r/1999ebjPbt` → POST 200 with title/duration/HD+SD URLs, `dl=hd`/`dl=sd` stream 200 video/mp4 (~665KB). Cover still empty on dev IP (documented limitation).
- **Photo fetch speedup (user report: "process takes too long").** `src/lib/facebook.ts`: photo path no longer waits sequentially on two full page downloads — (1) `fetchPhotoPage` now reads the response **bounded-stream** (`readBoundedText`, caps at 2.5MB, cancels the body reader once enough HTML is in — og:image lives in `<head>`, JSON blobs shortly after) with a 12s timeout (was 20s + full `resp.text()` of multi-MB pages); (2) desktop + mobile pages now fetch **in parallel** via `Promise.allSettled`, first page yielding a usable image wins — worst case (flagged dev IP) went from ~40s of sequential timeouts to a hard ~2s. Verified: photo POST on dev IP now 1.9s (error is the documented shell limitation).
- **Per-tool result tiles (user requests):** each FB tool's result card now shows ONLY its own download button(s) — `FacebookDownloadForm` JS: `tiles = isMp3Mode ? [audio] : [hd, sd]` (verified in compiled bundle `f?[H]:[F,j]`). MP3 page = audio tile (FAQ `mp3-video-with` → points to video/reels pages); video page = HD + SD (FAQ `dl-reels`/`dl-mp3`/`dl-live`, hero sub, "How the Facebook video downloader works", "Extract Facebook audio as MP3", "What quality and formats", live replays, "Where do downloaded videos go", step 03 all updated to drop the Audio tile and link `/facebook-to-mp3`); reels page = HD + SD (FAQ `reels-audio`, "Save Reel audio as MP3 too", step 03 → `/facebook-to-mp3`); story page = HD + SD (article audio mentions + step 03 → `/facebook-to-mp3`); photo page = photo tile only. NO page shows all three tiles anymore.
- KNOWN DEV-IP LIMITATION (same as video cover): this dev machine IP gets shelled FB pages — photo.php desktop = 200 shell (~440KB) with NO og:image, NO `"image":{"uri":…}`, only reaction-emoji scontent assets (deliberately skipped); mobile = 888B error shell; post plugin = 400; graph oEmbed = html w/o thumbnail. On unflagged IPs, desktop photo.php pages carry og:image → extractor works (standard pattern used by all FB photo downloaders). Do NOT chase a dev-IP workaround.

Key implementation facts (IMPORTANT for future work):
- Production env setup artifacts exist: `.env.production.example`, `scripts/setup-server.sh`, `deploy/tiksavehub.service`. Node standalone server does NOT auto-load `.env` files → env must come from the process (systemd `EnvironmentFile=/etc/tiksavehub/env`). Set `CACHE_FILE=/var/lib/tiksavehub/media-cache.json` (outside the deploy dir; `dist/` is wiped on redeploy). yt-dlp standalone binary at `/usr/local/bin/yt-dlp` (self-update `yt-dlp -U`). Site NOT launched yet — artifacts wait for launch day.
- Full Facebook downloader module built: `src/lib/facebook-url.ts`, `src/lib/facebook.ts`, `src/lib/ytdlp.ts` (facebook helpers), `src/pages/api/facebook.ts` (POST metadata + GET dl=hd|sd|audio with ffmpeg MP3), `src/components/FacebookDownloadForm.astro`, `src/pages/facebook-video-downloader.astro` (SEO landing + JSON-LD).
- Wired into `astro.config.mjs` (sitemap), `NavBar.astro`, `Footer.astro`, `Layout.astro`, `src/lib/pageSections.ts`.
- `astro check`: 0 errors/hints. `npm run build`: green. Live API smoke tests pass.

Key implementation facts (IMPORTANT for future work):
- Extraction order: page JSON (`playable_url_quality_hd`/`playable_url`) → embed plugin page `"videoData"[{hd_src,sd_src}]` (works even on FB login-wall/IP-flagged shells; direct progressive MP4s) → yt-dlp fallback.
- Duration on embed path decoded from base64url `efg` payload in CDN stream URLs (`durationFromEmbedConfig`, regex `&efg=([A-Za-z0-9_-]+)`, padded `atob` decode, `duration_s` field).
- CDN streams need `Referer: https://www.facebook.com/` to download (verified 200, video/mp4).
- Test video used everywhere: `https://www.facebook.com/facebook/videos/10153231379946729/` (dur 74s).
- Dev machine IP gets FB "Error" shell (HTTP 400, ~3.6KB) on **mobile** page fetch, but the **desktop** page fetch returns the FULL page (status 200, ~950KB) with real `<title>`/`og:title`/`meta description` — yet NO `preferred_thumbnail`, NO `og:image`, NO `playable_url*` keys on this IP. Production IPs get the full JSON via the mobile page (title/cover/counts via `preferred_thumbnail`).
- Embed plugin page (previous session): title/author now extracted from its markup — anchor `watch/?ref=embed_video">TITLE</a>` and `/watch/{page}/?ref=embed_video` link text; page/author avatar at s40x40 is size-bumped via `s40x40→s320x320` in the CDN `stp` param.
- `graph.facebook.com/v26.0/oembed_video?url=` works WITHOUT auth on public videos (returns title/author/date in the `html` field; NO thumbnail). Not currently used in code.
- Public metadata probe `fetchDesktopPageMeta(url)` in `facebook.ts` runs in parallel with `fetchEmbed` and fills `cover` (via `preferred_thumbnail`) + page name when the desktop page is served even though the mobile page shelled. Never throws.
- FB tool suite (built across sessions): 5 pages (`/facebook-video-downloader`, `/facebook-reels-downloader` = MAIN brand tab, `/facebook-story-downloader`, `/facebook-to-mp3`, `/facebook-photo-downloader`) sharing `FacebookDownloadForm` (mode prop + tabLinks incl. `main:true` Reels brand pill `fb-tab-main`/`fb-tab-pulse`) + 7 Fb* section components. NavBar FB links → reels (desktop `nav-link-fb` L37, mobile `mob-link-fb` L127).

Known limitation (documented, no action planned):
- On the embed fallback path with a fully flagged IP (like this dev machine), `cover` is still empty — no source exposes an og:image/thumbnail for such IPs (desktop page has no preferred_thumbnail; oEmbed has no thumbnail; the ambiguous `t15.5256-10` preloads are related-reels images and are deliberately NOT used). Result card now shows a graceful icon placeholder instead of a broken `<img>` when cover is empty.
